{
  "mcpServers": {
    "duckdb": {
      "type": "stdio",
      "command": "uvx",
      "args": [
        "mcp-server-motherduck",
        "--db-path",
        "${CLAUDE_PROJECT_DIR:-.}/data/warehouse.duckdb"
      ]
    }
  }
}
